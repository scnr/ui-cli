source 'https://rubygems.org'

gem 'rake', '~> 10.0'
gem 'pry'

group :spec do
    gem 'rspec', '~> 3.0'
end

group :prof do
    gem 'scnr-monitor', path: '../monitor'
end

gem 'scnr-engine', path: '../engine'

gemspec
